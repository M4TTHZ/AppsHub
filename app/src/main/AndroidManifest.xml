<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions for network access -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Notification permission for Android 13 and above -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppsHub">
        <activity
            android:name=".RoomDatabase.ui.RoomDatabaseActivity"
            android:exported="false" />
        <activity
            android:name=".Localizacoes.LocationActivity"
            android:exported="false" />
        <activity
            android:name=".Notificacoes.NotificationsActivity"
            android:exported="false" />
        <activity
            android:name=".SQLite.SQLiteActivity"
            android:exported="false" />
        <activity
            android:name=".Sharedpreferences.SharedPreferencesActivity2"
            android:exported="false" />
        <activity
            android:name=".Sharedpreferences.SharedPreferencesActivity"
            android:exported="false" />
        <activity
            android:name=".ClimateWeather.ClimateWeatherActivity"
            android:exported="false" />
        <activity
            android:name=".Sorteador.SorteadorActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>